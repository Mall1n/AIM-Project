using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject LoadedEngine = Resources.Load("Ship Assets/Engines/First Engine", typeof(GameObject)) as GameObject;
        //PrefabUtility.InstantiatePrefab(LoadedEngine);
    }

    // Update is called once per frame
    void Update()
    {

    }
}
